# Giao diện
customtkinter

# AI & Môi trường
google-genai
python-dotenv

# Xử lý Video
# Cài đặt MoviePy (các dependencies như numpy, proglog, decorator, tqdm sẽ tự động được cài)
moviepy>=2.0.0

# Xử lý Video: Thư viện bắt buộc cho MoviePy để đọc/ghi video (được khuyên dùng)
imageio
imageio-ffmpeg

# Tự động hóa trình duyệt (TikTok Upload)
playwright
playwright-stealth

# Khác
pillow
